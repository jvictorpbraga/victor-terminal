// Welcome hero — visible only when there are no messages yet.

export default function Welcome() {
  return (
    <div className="welcome">
      <h1 className="welcome-title">How can I help you today?</h1>
      <p className="welcome-sub">Ask anything. Attach images or files with the + button.</p>
    </div>
  );
}
